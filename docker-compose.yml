services:
  dataclaw:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ${DOCKER_DATA_DIR:-./docker-data}/dataclaw:/root/.dataclaw
    env_file:
      - path: .env
        required: false
