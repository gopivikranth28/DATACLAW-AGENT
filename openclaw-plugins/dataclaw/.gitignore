dist/
node_modules/

# User-specific. Generated by Dataclaw's openclaw install service from the
# live tool registry. The committed `tool-manifest.ts` re-exports from
# this file; the `prebuild` npm script creates an empty placeholder when
# it is missing so esbuild and tsc still resolve the import.
src/tools/tool-manifest.generated.ts
